<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.partyum.partyummanager.document.Command" />

        <variable
                name="viewModel"
                type="com.partyum.partyummanager.document.DocumentViewModel" />
    </data>

    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

        <LinearLayout
                android:layout_margin="20dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

            <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{viewModel.document.name}"
                    android:textSize="30sp" />

            <View
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_weight="1" />

            <Button
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/document_export"
                    android:textColor="@color/black"
                    android:layout_gravity="end"
                    android:onClick="@{()->viewModel.onClick(Command.EXPORT)}"
                    android:background="?attr/selectableItemBackground" />

            <Button
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/document_send_to_db"
                    android:layout_gravity="end"
                    android:textColor="@color/black"
                    android:onClick="@{()->viewModel.onClick(Command.SAVE_TO_FIREBASE)}"
                    android:background="?attr/selectableItemBackground"

                    />

        </LinearLayout>

        <LinearLayout
                android:background="@drawable/view_border"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="20dp"
                android:orientation="vertical">

            <ViewStub
                    android:id="@+id/document_form"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:viewModel="@{viewModel}" />

        </LinearLayout>

    </LinearLayout>

</layout>